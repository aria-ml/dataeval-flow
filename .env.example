# Copy to .env and customize paths for your environment
DATASET_DIR=/path/to/dataset
MODEL_DIR=/path/to/models
INCOMING_DIR=/path/to/incoming/images
OUTPUT_DIR=/path/to/output

# For DatasetDict (multi-split datasets), specify which split to use
# Leave empty or comment out for single-split Dataset
# DATASET_SPLIT=test
