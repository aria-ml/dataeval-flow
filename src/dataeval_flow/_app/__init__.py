"""Interactive configuration builder for DataEval Workflows.

The TUI (Textual) app requires ``pip install dataeval-flow[app]``.
The lightweight CLI builder uses ``click`` (included with ``datasets``).
"""
