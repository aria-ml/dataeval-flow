# JATIC Compliant GitLab CI/CD Pipeline
# Reference: docs/design/JATIC project software requirements.md

stages:
  - lint
  - security
  - test
  - build
  - release

# GitLab Security Templates [DSOR-1 to DSOR-5]
include:
  - template: Security/SAST.gitlab-ci.yml
  - template: Security/Dependency-Scanning.gitlab-ci.yml
  # Container Scanning requires a remote registry, so we disable it for this local-only workflow
  # - template: Security/Container-Scanning.gitlab-ci.yml
  - template: Security/Secret-Detection.gitlab-ci.yml

variables:
  UV_CACHE_DIR: "$CI_PROJECT_DIR/.uv-cache"
  DOCKER_TLS_CERTDIR: "/certs"
  
  # Harbor Registry Configuration
  # HARBOR_TOKEN must be set as a CI/CD Variable in GitLab
  HARBOR_URL: "harbor.jatic.net"
  HARBOR_USER: "robot$dataeval+dataeval-app"
  
  # Target Image Path
  # Used for local tagging only in this configuration
  CI_APPLICATION_REPOSITORY: "$HARBOR_URL/dataeval/dataeval-app"
  CI_APPLICATION_TAG: "$CI_COMMIT_SHA"

# -----------------------------------------------------------------------------
# Lint Stage
# -----------------------------------------------------------------------------

lint:
  stage: lint
  image: ghcr.io/astral-sh/uv:python3.11-bookworm-slim
  script:
    # Use uvx to run nox with the nox-uv plugin
    - uvx --with nox-uv nox -s lint
  cache:
    key: uv-lint-$CI_COMMIT_REF_SLUG
    paths:
      - .uv-cache
      - .venv
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# Type Check [TR-7-H-1, TR-7-H-2]
# Type check should be an explicit, visible job in the pipeline, not hidden in lint
type-check:
  stage: lint
  image: ghcr.io/astral-sh/uv:python3.11-bookworm-slim
  script:
    # Use uvx to run nox with the nox-uv plugin
    - uvx --with nox-uv nox -s type
  cache:
    key: uv-type-$CI_COMMIT_REF_SLUG
    paths:
      - .uv-cache
      - .venv
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# -----------------------------------------------------------------------------
# Test Stage [TR-1-H-4: Python 3.10, 3.11, 3.12]
# -----------------------------------------------------------------------------

test:
  stage: test
  image: ghcr.io/astral-sh/uv:python${PYTHON_VERSION}-bookworm-slim
  parallel:
    matrix:
      - PYTHON_VERSION: ["3.10", "3.11", "3.12"]
  script:
    # Install 'nox-uv' alongside 'nox' so the noxfile imports work
    - uvx --with nox-uv nox -s test
  coverage: '/TOTAL.*\s+(\d+%)/'
  artifacts:
    paths:
      - output/
    reports:
      junit: output/junit.xml
      coverage_report:
        coverage_format: cobertura
        path: output/coverage.xml
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# -----------------------------------------------------------------------------
# Build Stage (Local Build Only - No Push)
# -----------------------------------------------------------------------------

build:docker:
  stage: build
  # Update image to match working base.yml reference
  image: docker:25.0.5-git
  services:
    - docker:24.0-dind
  # Login removed: We are not pushing to Harbor, so login is unnecessary
  # before_script:
  #   - apk add --no-cache bash util-linux
  #   - echo ${HARBOR_TOKEN} | docker login ${HARBOR_URL} -u "${HARBOR_USER}" --password-stdin || echo "Failed to login to Harbor..."
  script:
    # Build GPU image (Uses unified Dockerfile with override args)
    # Flattened command to avoid line-continuation syntax errors in CI runner
    - docker build --build-arg BASE_IMAGE="nvidia/cuda:11.8.0-cudnn8-runtime-ubuntu22.04" --build-arg CONTAINER_MODE="gpu" -t $CI_APPLICATION_REPOSITORY:$CI_APPLICATION_TAG -t $CI_APPLICATION_REPOSITORY:gpu .
        
    # Build CPU image (Uses unified Dockerfile with defaults)
    - docker build -t $CI_APPLICATION_REPOSITORY:cpu .
    
    # Push commands removed to avoid Harbor authentication errors
    # - docker push $CI_APPLICATION_REPOSITORY:$CI_APPLICATION_TAG
    # - docker push $CI_APPLICATION_REPOSITORY:gpu
    # - docker push $CI_APPLICATION_REPOSITORY:cpu
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# Container Smoke Test - Validates entrypoint.sh executes correctly
container-test:
  stage: build
  image: docker:25.0.5-git
  services:
    - docker:24.0-dind
  needs: [build:docker]
  script:
    # Create minimal test config (required mount)
    - mkdir -p /tmp/test-config
    - |
      cat > /tmp/test-config/config.yaml << 'EOF'
      datasets:
        - name: test
          format: huggingface
          path: /data/dataset
          splits: [train]
      EOF

    # Create minimal test dataset (required mount)
    - mkdir -p /tmp/test-dataset
    - echo "test" > /tmp/test-dataset/dummy.txt

    # Build CPU image locally (can't pull from registry in this setup)
    - docker build -f Dockerfile.cpu -t dataeval-test:cpu .

    # Smoke test: Run with --help flag (triggers entrypoint help display)
    - |
      docker run --rm \
        -v /tmp/test-config:/data/config:ro \
        -v /tmp/test-dataset:/data/dataset:ro \
        dataeval-test:cpu --help

    # Verify exit code 0
    - echo "Container smoke test passed"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# GitLab Pages - Publish Sphinx documentation
pages:
  stage: build
  image: ghcr.io/astral-sh/uv:python3.11-bookworm-slim
  script:
    - uvx --with nox-uv nox -s docs
    - mv output/docs public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

# -----------------------------------------------------------------------------
# Security Stage [DSOR-1 to DSOR-5]
# -----------------------------------------------------------------------------

# SAST - Static Application Security Testing [DSOR-1-H-1, DSOR-1-H-2]
semgrep-sast:
  stage: security
  variables:
    SAST_EXCLUDED_ANALYZERS: "eslint,gosec,kubesec,nodejs-scan,phpcs-security-audit,pmd-apex,security-code-scan,sobelow,spotbugs"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# Dependency Scanning [DSOR-2-H-1, DSOR-2-H-2]
gemnasium-python-dependency_scanning:
  stage: security
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# Secret Detection [DSOR-4-H-1, DSOR-4-H-2]
# This job name is valid in the template
secret_detection:
  stage: security
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# SBOM Export [DSOR-5-H-1]
sbom:
  stage: security
  # Override entrypoint so GitLab can use shell instead of running syft immediately
  image: alpine:latest
  script:
    - apk add --no-cache curl
    - curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin
    - /usr/local/bin/syft dir:. -o cyclonedx-json=sbom.json
  artifacts:
    paths:
      - sbom.json
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# -----------------------------------------------------------------------------
# Release Stage
# -----------------------------------------------------------------------------

# Tag release images
# DISABLED: Cannot tag a release if the image was never pushed to the registry.
# release:tag:
#   stage: release
#   # Update image to match working base.yml reference
#   image: docker:25.0.5-git
#   services:
#     - docker:24.0-dind
#   before_script:
#     # Matches logic from base.yml
#     - apk add --no-cache bash util-linux
#     - echo ${HARBOR_TOKEN} | docker login ${HARBOR_URL} -u "${HARBOR_USER}" --password-stdin || echo "Failed to login to Harbor..."
#   script:
#     - docker pull $CI_APPLICATION_REPOSITORY:$CI_APPLICATION_TAG
#     # Tag with git tag (e.g. v1.0.0)
#     - docker tag $CI_APPLICATION_REPOSITORY:$CI_APPLICATION_TAG $CI_APPLICATION_REPOSITORY:$CI_COMMIT_TAG
#     - docker push $CI_APPLICATION_REPOSITORY:$CI_COMMIT_TAG
#   rules:
#     - if: $CI_COMMIT_TAG