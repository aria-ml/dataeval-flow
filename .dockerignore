# =============================================================================
# Docker build context — only include what Dockerfiles actually COPY:
#   pyproject.toml, uv.lock, noxfile.py, src/, tests/, config/, entrypoint.sh, README.md
# =============================================================================

# Version control
.git
.gitattributes
.gitignore
.github

# CI/CD and Docker config (not needed inside images)
.gitlab-ci.yml
docker/
!docker/entrypoint.sh
# Scan report and SBOM, written here by CI between the `prod` and `scanned`
# builds. Absent on a plain local build, which only targets `prod`.
!docker/security
!docker/security/**

# Python artifacts
.venv
.nox
__pycache__
*.pyc
*.pyo
*.egg-info
.coverage
.pytest_cache
.ruff_cache

# IDE and tooling
.claude
.vscode
.idea
*.swp
*.swo

# Environment and secrets
.env
.env.*
cosign.key

# Documentation and metadata (not needed at runtime)
docs/
LICENSE
*.md
!README.md

# Build output
output/
htmlcov

# Local data (mounted at runtime, not baked in)
data/

# Misc
.cuda-version
